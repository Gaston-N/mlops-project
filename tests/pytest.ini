[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*

# verbose output and short tracebacks as default
addopts = -v --tb=short

filterwarnings =
    ignore::DeprecationWarning
